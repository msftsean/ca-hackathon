[project]
name = "multilingual-emergency-chat"
version = "0.1.0"
description = "Multilingual emergency information assistant for Cal OES"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
