[project]
name = "genai-procurement-compliance"
version = "0.1.0"
description = "Automated vendor attestation review and compliance scoring for CDT/DGS"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
