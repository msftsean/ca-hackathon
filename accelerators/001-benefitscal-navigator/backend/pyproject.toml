[project]
name = "benefitscal-navigator"
version = "0.1.0"
description = "AI-powered benefits eligibility assistant for CDSS programs"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
