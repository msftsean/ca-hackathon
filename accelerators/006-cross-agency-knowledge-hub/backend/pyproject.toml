[project]
name = "cross-agency-knowledge-hub"
version = "0.1.0"
description = "Federated search across California state government knowledge bases"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
