[project]
name = "permit-streamliner"
version = "0.1.0"
description = "AI-powered permit intake and routing for OPR, HCD, and DCA"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
