[project]
name = "edd-claims-assistant"
version = "0.1.0"
description = "AI-powered claims status and eligibility assistant for EDD"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
